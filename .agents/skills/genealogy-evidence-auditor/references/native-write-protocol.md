# Native Write Protocol

Before writing: read live policies, affected notes, linked evidence, repository state when available, unrelated changes, and prepare a minimum patch.

After writing: preserve frontmatter/native confidence, update `last-reviewed` where appropriate, synchronize material project-state changes, review diff, and report exact changed paths.

Do not commit, push, reset, clean, discard unrelated changes, or create a PR without explicit authorization.
