interface:
  display_name: "Hamrick Genealogy Evidence Auditor"
  short_description: "Vault-native genealogy audit with integrity preflight"
  default_prompt: "Work vault-first, audit atomic claims, preserve exact source forms and unsupported bridges, use native Hamrick confidence labels, exclude GEDCOM from canonical evidence unless explicitly requested, and run read-only integrity/schema checks before consequential writes when available."
